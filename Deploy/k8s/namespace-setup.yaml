# Production Namespace
apiVersion: v1
kind: Namespace
metadata:
  name: production
  labels:
    name: production
    environment: production
    managed-by: argocd
---
# Network Policy for Production Namespace (optional security)
# apiVersion: networking.k8s.io/v1
# kind: NetworkPolicy
# metadata:
#   name: production-network-policy
#   namespace: production
# spec:
#   podSelector: {}
#   policyTypes:
#   - Ingress
#   - Egress
#   ingress:
#   - from:
#     - namespaceSelector:
#         matchLabels:
#           name: production
#     - namespaceSelector:
#         matchLabels:
#           name: argocd
#     - namespaceSelector:
#         matchLabels:
#           name: ingress-nginx
#   egress:
#   - {} # Allow all egress traffic
---
# Resource Quota for Production Namespace
# apiVersion: v1
# kind: ResourceQuota
# metadata:
#   name: production-quota
#   namespace: production
# spec:
#   hard:
#     requests.cpu: "4"
#     requests.memory: 8Gi
#     limits.cpu: "8"
#     limits.memory: 16Gi
#     persistentvolumeclaims: "5"
#     pods: "10"
#     services: "5"
#     secrets: "10"
#     configmaps: "10"